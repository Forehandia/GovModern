---
interface Props {
  class?: string;
}

const { class: extraClass = '' } = Astro.props;
---

<div class:list={[
  'bg-white rounded-2xl p-8 shadow-sm border border-gray-100 hover:shadow-md transition-shadow',
  extraClass,
]}>
  <slot />
</div>
